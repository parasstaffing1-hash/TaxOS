services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taxos-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    networks:
      - taxos-network
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: taxos-db
    environment:
      POSTGRES_USER: taxos
      POSTGRES_PASSWORD: taxos
      POSTGRES_DB: taxos
    ports:
      - "5432:5432"
    volumes:
      - taxos-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taxos"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - taxos-network
    restart: unless-stopped

volumes:
  taxos-db-data:

networks:
  taxos-network:
    driver: bridge
