# Browser requests use this value. Leave relative when the frontend reverse-proxies /api/v1.
NEXT_PUBLIC_API_URL=/api/v1
# Server-rendered routes and builds need an absolute backend URL.
TAXOS_API_URL=http://localhost:8000/api/v1
